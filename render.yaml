services:
  - type: web
    name: sentinel-demo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind=0.0.0.0:$PORT --workers=2 --timeout=120
    healthCheckPath: /simple/
    autoDeploy: yes
